'use babel';

import WeatherInformationView from '../lib/weather-information-view';

describe('WeatherInformationView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
